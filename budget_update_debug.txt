if(next&&validateStep())showStep(activeStep+1)});form.addEventListener("keydown",function(event){if(event.key==="Enter"&&event.target.tagName!=="TEXTAREA"&&activeStep<steps.length-1){event.preventDefault();if(validateStep())showStep(activeStep+1)}});if(budgetRange)budgetRange.addEventListener("input",updateBudget);form.addEventListener("submit",function(event){event.preventDefault();if(!validateStep())return;const submit=form.querySelector('button[type="submit"]');const data=new FormData(form);const services=Array.from(form.querySelectorAll(".cta-project-service-option.is-selected")).map(function(item){return item.dataset.service});const message=String(data.get("message")||"").trim()||"Proje
